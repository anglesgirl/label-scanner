# AGENTS.md — LabelScanner 项目约定

## 项目定位
机器标签拍照 → 扫码/OCR → 自动填字段 → 导出库存导入模板 Excel。
用户是工厂仓储/计划岗，App 给自己和同事用，**无云 AI 额度，全离线**。

## 硬性约束
- **全离线**：ML Kit 本地模型，禁止任何云端 OCR/识别 API。
- **中文界面**：所有 UI 文案中文。
- **不要乱改模板列规则**：D=物料编码 E=箱号SN F=数量 I=生产日期(yyyymmdd)
  O=SN码(同E)；第 1/2 行列名与表头不可改（导入系统硬性要求）。
- 物料编码：10 位补 01；12 位原样；**69 开头系统不认但用户要用**——App
  存完整数据，导出时生成两份（系统版剔除 69、自有版全保留）。

## 架构
- View 体系（非 Compose）：MainActivity + CameraX PreviewView +
  ImageAnalysis → BarcodeAnalyzer（ML Kit 双引擎：条码多码 + 中文 OCR）
- 解析纯规则：`LabelParser.classify()` 按值特征分类，不依赖标签排版
- 结果流：analyze 帧 → 合并解析 → LabelResult → UI 人工确认 → 列表

## 已知注意
- ML Kit barcode 17.3.0 / text-recognition 16.0.1 / cameraX 1.6.1 /
  Gradle 9.4.1 / AGP 8.11.1 / compileSdk 36 / minSdk 26
- 本机无 Android SDK，构建靠 GitHub Actions（assembleDebug → artifact）
- 别升级 OkHttp 之类的传递依赖导致 R8 问题（参照 Han1meViewer 教训）


## 特殊情况与已知限制（勿重复投入）

### 高密度 Data Matrix 码（部分 `PA...` 标签上三并排的 DM 码）

现象：**手机扫不出来，只有扫码枪能扫**（用户 2026-09 实测）。

原因（**物理限制，非实现缺陷**）：
- Data Matrix 的模块极小，可靠解码需**每模块 ≥3 像素**
- 手机**实时预览帧**通常 640×480 / 1280×720 → 每模块不足 1 像素 → **必然失败**
- 扫码枪专为近距高密度码设计（高分辨率 + 短景深），所以能读

结论：
- **不作为主线需求**（用户明确：属特殊情况，日常采集用不到）
- 唯一可行路径：**全分辨率拍照 → 裁到码区 → 放大 → zxing-cpp 强通道**
- **实时扫描无论怎么调参都不可能解出**（帧分辨率是硬上限，别再尝试）
- 真遇到时走「添加序列号」入口，**解不出允许手工输入**，不阻断流程

### 物料编码 ⟷ 69 码

二者**一一对应**。UI 合并为一个字段、按钮切换显示；识别到任一即反查另一个。
OCR 常见误认需先归一化：`O↔0`、`S↔5`、`l↔1`、`Z↔2`。

### 快速操作相机（旧版遗留的三个入口，各有取舍）

| 入口 | 谁在拍 | 优 | 缺 |
|---|---|---|---|
| 系统相机 | 手机原生 | 最清晰 | 无任何识别能力 |
| 文档模式（自研） | App 自己 | 有框选/自动拍/矫正 | 有缺陷（抖动/对不准） |
| 相册 | 系统相机或**系统文档模式** | **画质与矫正最好** | 要手动导入 |

注意：**系统文档模式（如小米/华为的文档扫描）是厂商私有能力，第三方 App 调不到**，
只能"拍完再导入"。因此「相册/导入」路径在画质上反而是最优解。

## 【铁律】采集层不做取舍 —— 先全量获得，再由上层分析

数据采集功能（扫码 / OCR / 识别）的**唯一职责是把这一屏的数据全部拿到**，
**绝不在这一层做挑选、取第一个、按长短留一个**。

- 反例：`values.firstOrNull()` —— 谁先解出来就用谁。标签上 1D 条码总比 2D 集成码
  先解出，集成码就永远拿不到，用户只能用手把别的码遮住，还以为是"识别不出来"。
- 反例：`list[0]` —— 上层明明已经拿到多个码，却只填第一个，其余直接丢弃。
- 正例：同帧所有码全部收下（带 1D/2D 类型标注）→ 回传上层 → 上层筛选或交用户确认。

**为什么**：`先到先得` 是分析层的偷懒，塞进采集层就变成静默丢数据。
丢掉的码现场看不出来，事后核对比重采还贵。

**推论**：
1. 没有"取不到"，只有"选错了"。排查取码问题先确认**拿到了什么**，再查选取逻辑。
2. 诊断日志必须记录**同帧全部码值**，而不只是"扫到几个码"——只记数量等于没有观测。
3. 去重（`distinct`）可以，因为它不丢不同数据；但"留一个 / 留最长 / 留第一个"不行。
